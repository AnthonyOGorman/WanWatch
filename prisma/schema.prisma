generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Settings {
  id                  Int      @id
  pollIntervalSeconds Int      @default(300)
  retentionDays       Int      @default(90)
  enabled             Boolean  @default(true)
  ipProvider          String   @default("ipify")
  pollTimeoutSeconds  Int      @default(5)
  webhookUrl          String?
  updatedAt           DateTime @updatedAt
}

model WanIpLog {
  id         Int      @id @default(autoincrement())
  ts         DateTime @default(now())
  ip         String?
  provider   String
  ok         Boolean
  responseMs Int?
  error      String?
  isp        String?
  country    String?

  @@index([ts])
}

model PollRequest {
  id          Int       @id @default(autoincrement())
  requestedAt DateTime  @default(now())
  handledAt   DateTime?
  handledBy   String?
}
